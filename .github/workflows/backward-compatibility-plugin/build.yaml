name: "Fluxnova Backward Compatibility Plugin Build"
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - "fluxnova-backward-compatibility-plugin/**"
      - ".github/**"
  workflow_dispatch:

concurrency:
  group: ${{ github.ref == 'refs/heads/main' && format('ci-default-branch-{0}-{1}', github.sha, github.workflow) ||
    format('ci-pr-{0}-{1}', github.ref, github.workflow) }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/common-build.yaml
    with:
      project-name: fluxnova-backward-compatibility-plugin
      project-path: fluxnova-backward-compatibility-plugin
    secrets: inherit